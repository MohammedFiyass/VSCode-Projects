# playbook to remove the nginx package and stop the services in redhat linux
---
- name: Remove Nginx and stop the service on RedHat Linux
  hosts: all
  become: yes

  tasks:
    - name: Stop Nginx service
      service:
        name: nginx
        state: stopped

    - name: Remove Nginx package
      yum:
        name: nginx
        state: absent